import React from "react";
import { SafeAreaView, Text } from "react-native";

const Messages = () => {
  return (
    <SafeAreaView className={"flex-1 bg-background"}>
      <Text className={"text-5xl mx-auto mt-24 text-white text-center"}>
        Messages
      </Text>
    </SafeAreaView>
  );
};

export default Messages;
